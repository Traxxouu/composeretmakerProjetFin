{% extends 'base.html.twig' %}

{% block title %}Nouveau Produit - EfreiTech{% endblock %}

{% block body %}
    <div class="max-w-3xl mx-auto">
        <div class="mb-6">
            <a href="{{ path('app_produit_index') }}" class="text-purple-600 hover:text-purple-800 font-semibold">
                ← Retour à la liste des produits
            </a>
        </div>

        <div class="content-container">
            <div class="mb-8">
                <h1 class="text-3xl font-bold text-gray-800 mb-2">Ajouter un nouveau produit</h1>
                <p class="text-gray-600">Remplissez les informations ci-dessous pour créer un nouveau produit</p>
            </div>

            {{ include('produit/_form.html.twig', {'button_label': 'Créer le produit'}) }}
        </div>
    </div>
{% endblock %}
